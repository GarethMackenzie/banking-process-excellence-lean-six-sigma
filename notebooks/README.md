# Notebooks

The canonical analysis is intentionally implemented as executable scripts under `scripts/` so CI can reproduce it deterministically. This folder is reserved for optional exploratory notebooks; tracked results are generated from the script pipeline rather than hidden notebook state.
