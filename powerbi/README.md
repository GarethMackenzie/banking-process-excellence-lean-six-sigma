# Power BI integration

Power BI is a presentation layer for this case study, not the evidence source. The repository does not fabricate PBIX/PBIP files or screenshots. A future Desktop build should use `data/clean/applications_clean.csv` plus the capacity, MSA and DOE outputs after running the local pipeline.
